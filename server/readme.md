# Install

pip install pipenv
pipenv install
pipenv shell

# Run the server

pipenv shell
python3 main.py

# To generate the openai.json file

Make a call to /generateOpenAPIDefinition API
